#include <stdio.h>

/**
 * main - Entry point
 * @agrc: agument count
 * @agrv: agument vector
 * Return: 0 success
 */
int main (int agrc, char *argv[])
{
	agrc = 0;
	printf("%s\n", argv[agrc]);
	return (0);
}
