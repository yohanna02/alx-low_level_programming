#include "main.h"

/**
 * _abs - return the absolute value of a number
 * @n: The number to get the absolute value
 *
 * Return: the absolute value of a number
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
